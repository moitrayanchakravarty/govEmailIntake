# src/components/review/

Components specific to the "review" feature/domain — e.g. a queue table and
a detail panel (`ReviewQueueTable.jsx`, `RequestDetailPanel.jsx`) used by
the review workflow pages.

Same pattern as `components/registry/` — feature-scoped, imported by the
relevant pages, not reused outside this feature's context.
